# Backend API (Nest) — used for login/signup and all ERP calls
VITE_API_BASE_URL=http://localhost:5000

# Marketing site — used for post-logout / “back to site” links
VITE_LANDING_BASE_URL=http://localhost:5174
